---
driver:
  name: vagrant
  customize:
    natdnshostresolver1: "on"
    
provisioner:
  name: chef_zero

# Uncomment the following verifier to leverage Inspec instead of Busser (the
# default verifier)
# verifier:
#   name: inspec
#   format: doc

platforms:
  - name: centos-7.2

suites:
  - name: workstation
    run_list:
      - recipe[chef-mre::download-packages]
    attributes:
